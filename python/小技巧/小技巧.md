```Python
input = ['Duration', 'F0', 'F1', 'F2', 'F3']

# method 1
{f:i for f, j in zip(input, range(len(input)))}

# method 2
dict(zip(input, range(len(input))))

# method 3
dict(enumerate(input))

#
```
